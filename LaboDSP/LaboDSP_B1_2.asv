clear all;
close all;

r=400;
p=4;
Fs=r;
Ts=1/Fs;
t = (0:(n-1))*Ts;
df=(0:(n-1))*Fs/n;
ruis=rand();
x=sin(2*pi*p*t)+ruis;

a1=[-0.1 -0.5 -0.9 -1.0 -1.1];
y_1=0;
ones=ones(1,200);

for i=1:5
    y=filter(1,[1,a1(i)*x],ones);
    figure(i)
    plot(y)
end

% for i =1:5
%     for r = 1:4
%         y=x+a1(i)*y_1
%         y_1=y;
%     end
%     fprintf('%d: \n',i)
% end
